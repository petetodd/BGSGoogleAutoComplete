# Uncomment this line to define a global platform for your project
platform :ios, '8.0'

# In order to use CocoaPods written in Swift, you must explicitly include use_frameworks! to opt into using frameworks.
use_frameworks!

target 'BrightTester' do
    pod 'GoogleMaps'

end

